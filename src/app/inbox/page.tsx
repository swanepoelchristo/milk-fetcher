// if your tsconfig has "@/..." alias (most Next apps do)
import MoveForm from "@/components/MoveForm";

// if that fails, use a relative import instead:
// import MoveForm from "../../components/MoveForm";
